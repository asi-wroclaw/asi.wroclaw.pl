---
pageable: true
per_page: 10
---
- page_articles.each_with_index do |article, _i|
  .post
    %h3
      .row.justify-content-between
        .col-md-8.col-sm-12
          = link_to article.title, article
        .col-md-4.col-sm-12.text-md-end
          %small.text-muted= I18n.localize(article.date, format: '%Y-%m-%d')
    %p
      = article.summary(250)

- if paginate && num_pages > 1
  %p= "Strona #{page_number} z #{num_pages}"
  - if prev_page
    %p.pagination-controls.dx= link_to 'Poprzednia strona', prev_page
  - if paginate && next_page
    %p.pagination-controls.dx= link_to 'Następna strona', next_page
